ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
PORT=8787

# Use the same three names (not PORT) as Netlify environment variables.
# Site configuration → Environment variables. Never prefix them with VITE_.

